<script type="text/javascript" charset="utf-8">
    // 用于国际化
    var dft_form_title = "<%= t(:untitled_form) %>";
    var dft_form_desc = "<%= t(:describe_your_form) %>";
    var new_question_regex = /<%= t(:question) %> #\d/;
    var new_option_regex = /<%= t(:option) %> #\d/;
    var new_question_title = "<%= t(:question) %> #";
    var new_option_name = "<%= t(:option) %> #";
    var delele_link_text = "<%= t(:delete) %>";
    var other_text = "<%= t(:label_csi_survey_subjects_other) %>";
</script>
<style type="text/css">
  .stringSubject{
      border:1px dashed gray;
      cursor:default;
      padding:2px 3px;
      vertical-align:top;
      margin-bottom: 5px;
  }
  .textSubject{
      height:7em;
      width:35em;
      border:1px dashed gray;
      cursor:default;
      padding:2px 3px;
      vertical-align:top;
      margin-bottom:5px;
  }
</style>