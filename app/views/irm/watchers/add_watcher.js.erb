$(function(){
    $("#watcher_list").html('<%= escape_javascript(watchers_list(@watchable))%>');
    $("#watcher_select").html('<%= escape_javascript(select_tag "watcher", options_for_select(ava_watchers(@watchable.id, @watchable.class), {:id => "watcher"}))%>');
});